export const API_URL = "https://backend.url/api";

export const LOCAL_TIMEZONE: string = "Europe/Helsinki";
export const DATE_TIME_FORMAT: string = "fi-FI"

export const DATE_TIME_FORMAT_OPTIONS: Intl.DateTimeFormatOptions = {
    weekday: 'long',
    year: 'numeric',
    month: 'long',
    day: 'numeric',
    hour: 'numeric',
    minute: 'numeric'
}
